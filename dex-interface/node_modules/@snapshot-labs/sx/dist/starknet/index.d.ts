import { AddTransactionResponse } from 'starknet';
export declare function propose(space: string, executionHash: string, metadataUri: string): Promise<AddTransactionResponse>;
export declare function vote(space: string, proposal: string, choice: string): Promise<AddTransactionResponse>;
